{
  "name": "teachhub",
  "version": "1.0.0",
  "private": true,
  "description": "TeachHub 仓库根：统一 git hooks（husky + lint-staged + commitlint）",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10"
  }
}
