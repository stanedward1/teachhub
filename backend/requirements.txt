fastapi==0.115.4
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings>=2.2,<3
pyjwt==2.9.0
slowapi==0.1.9
bcrypt==4.1.2
python-multipart==0.0.17
python-dotenv==1.0.0
openpyxl==3.1.2
faker==25.8.0
alembic==1.13.2
pymysql==1.1.1
# AI 批改：HTTP 客户端（调用第三方 OpenAI 兼容接口）
httpx==0.27.2
# AI 批改：凭证对称加密（Fernet）
cryptography>=42,<51
# AI 批改：附件文本抽取
python-docx==1.1.2
pypdf==5.1.0
# AI 批改：图片预处理（按内容嗅探格式 / 预缩放 / EXIF 摆正 / 透明通道保留），延迟导入，
# Pillow 缺失时降级为「原始字节直送」，不阻塞批改。
Pillow>=10.3,<13
